# ========================================
# BACKEND ENVIRONMENT VARIABLES (EXAMPLE)
# ========================================
# Copy this file to `.env` and fill in your actual values
# NEVER commit the actual `.env` file!

# Server Configuration
SERVER_ADDRESS=0.0.0.0
SERVER_PORT=8080

# Database
DB_URL=jdbc:sqlite:examdb.db

# JWT Security
JWT_SECRET=GENERATE_A_LONG_RANDOM_STRING_HERE_AT_LEAST_64_CHARACTERS
JWT_EXPIRATION=86400000

# Email Configuration (Gmail)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-specific-password

# Librarian Email (who receives new student notifications)
LIBRARIAN_EMAIL=librarian@example.com

# ========================================
# HOW TO GET GMAIL APP PASSWORD:
# ========================================
# 1. Go to: https://myaccount.google.com/apppasswords
# 2. Select "Mail" and "Other (Custom name)"
# 3. Generate password
# 4. Use that 16-character password here (not your regular Gmail password)

